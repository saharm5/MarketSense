import { Routes } from '@angular/router';
import { TreeMapPageComponent } from './tree-map-page.component';

export const routes: Routes = [
    {
        path: '',
        component: TreeMapPageComponent
    }
];
